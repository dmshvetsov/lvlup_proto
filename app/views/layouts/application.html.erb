<!DOCTYPE html>
<html lang="en">
<head>
  <title>LvlupProto</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div class="container-fluid">
    <div class="row text-center">
      <div class="col-md-3 col-md-offset-3 logo">
        <span class="label label-default">LvlUp</span>
      </div>
      <div class="col-md-3 profile">
        <% if current_user %>
          name here | <%= link_to t('facebook_logout'), signout_path %>
        <% end %>
      </div>
    </div>
    <hr />
    <div class="row">
      <div class="col-md-12">
        <h1>
          header here
        </h1>
      </div>
    </div>
    <%= yield %>
  </div>

  <%= console %>
</body>
</html>
